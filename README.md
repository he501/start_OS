# start_OS

__実行方法__
  
```sh
sudo apt install -y git && git clone https://github.com/he501/start_OS && ./start_OS/start.sh
#(sudo dnf update -y && sudo dnf install -y git) || (sudo apt update && sudo apt upgrade -y && sudo apt install -y git) && git clone https://github.com/he501/start_OS && ./start_OS/start.sh
```
